run = "NLTK_DATA=/home/runner/nltk_data SKIP_WORDNET=1 python3 -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"

modules = ["python-3.11"]

[setup]
install = [
  "python3 -m pip install -q -r requirements.txt"
]

[deployment]
run = ["sh", "-c", "NLTK_DATA=/home/runner/nltk_data SKIP_WORDNET=1 python3 -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"]

[nix]
channel = "stable-24_05"
packages = ["libxcrypt", "rustc", "cargo"]

[[ports]]
localPort = 8080
externalPort = 80 