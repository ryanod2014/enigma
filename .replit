run = "bash -c 'cd web && npm install && npm run build && cd .. && uvicorn api.main:app --host 0.0.0.0 --port 8000'"

[deployment]
run = ["sh", "-c", "cd web && npm install && npm run build && cd .. && uvicorn api.main:app --host 0.0.0.0 --port 8000"]

packages = ["libxcrypt", "nodejs_20"] 